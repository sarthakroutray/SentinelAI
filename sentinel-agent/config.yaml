sentinel:
  server: http://localhost/api-proxy
  api_key: "dummy_key"
  batch_size: 2000
  flush_interval: 1
  request_timeout: 10
  max_backoff: 30

poll_interval: 0.25
queue_size: 10000

logs:
  - path: ../logfiles.log
    source: nginx
